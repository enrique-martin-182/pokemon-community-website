/**
 * Duración (en milisegundos) que se muestran los valores de la estadística
 * antes de pasar a la siguiente ronda (acierto) o a Game Over (fallo).
 */
export const REVEAL_MS_CORRECT = 2500 // cuando aciertas
export const REVEAL_MS_WRONG = 2500 // cuando fallas
